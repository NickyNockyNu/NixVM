globals
ordinals
begin/end
write/writeln (via _SysCall_DebugPrint)
if else
booleans
floats (single) 
  TODO: float built-in funcs
type casting
consts
while
repeat
for to downto
break
continue
enums/sets/in (TEnum=(one, two, three); TSetOfEnum=set of TEnum;)
case
pointers
  typed pointers
  dereferencing
  typed pointers - records fields/methods/properties
  typed pointers - arrays
procedures/functions
  local variables
  interrupts
  syscalls
  varargs
  method pointers
  result
  exit(result)
managed strings (via _SysCall_String*)
  lifetime/scope management
  string char indexing
  managed strings inside arrays/records
  length
  copy
  format
arrays
  multi-dimention arrays
records
  nested records/arrays
  record methods
  "self" inside record methods
  record properties
  pirvate/public visibility
  packed records
halt
yield
uses
program PPP(V, v) targets TTTT(V, v);
directives for heap, stack and base address
interface/implementation/initialization unit sections
